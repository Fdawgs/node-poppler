language: node_js

node_js:
  - "lts/*"
  - "node"

os: 
  - osx
  - windows

# safelist
branches:
  only:
  - master
  - develop
  - /^greenkeeper/.*$/

script: echo "Running tests against $(node -v)..."