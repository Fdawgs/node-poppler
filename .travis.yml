language: node_js

node_js:
  - "lts/*"
  - "node"

os: 
  - linux
  - windows

matrix:
  allow_failures:
    - os: windows


# safelist
branches:
  only:
  - master
  - develop
  - /^greenkeeper/.*$/

script: echo "Running tests against $(node -v)..."